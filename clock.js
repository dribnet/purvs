/* * us p5.js to draw a clock on a 960x500 canvas */ function draw_clock(hour, minute, second, millis, alarm) {background(157,206,177); // light gray backgroundstrokeWeight(4); // Stroke weight to 8 pixelsstroke(0);var size ;// The rectangle draws on top of the ellipse// because it comes after in the codefill(187,160,137);//ellipse(500,250,size);//text("Hour: " + hour, 10, 22);for(var i = 5; i > 0; i--){	ellipse(50, 50,i * 20);}//var pos;//pos = map(second. 0, 60, 0, 960);//ellipse(pos1, 250, 100);ellipse(500,250,size);strokeWeight(8);stroke(200);noFill();noStroke();}